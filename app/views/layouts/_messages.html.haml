- flash.each do |name, msg|
  - if msg.is_a?(String)
    %div{:class => "navbar-fixed-top alert alert-#{name == :notice ? "success" : "error"}"}
      %a.close{"data-dismiss" => "alert"} ×
      = content_tag :div, msg, :id => "flash_#{name}"
