= simple_form_for(@sell) do |f|
  = f.error_notification

  .form-inputs.row
    .span4.fortextarea
      = f.input :title
      = f.input :body, as: :text
    .span4
      = f.input :count
      = f.association :count_type
      = f.input :price
      / = f.input :price_type

      / #region and city selects
      = f.association :region, :prompt => "Выберите район"
      / = f.association :city
      .control-group.select.optional.sell_city
        %label.select.optional.control-label{ for: "sell_city_id" }
          Населенный пункт
        #sell_city
          = render :partial => "cities"

      / #category and subcategory selects
      = f.association :category, :prompt => "Выберите категорию"
      / = f.association :subcategory
      .control-group.select.optional.sell_subcategory
        %label.select.optional.control-label{ for: "sell_subcategory_id" }
          Подкатегория
        #sell_subcategory
          = render :partial => "subcats"

  .form-actions
    = f.button :submit, :class => 'btn btn-primary'
