= simple_form_for(@city) do |f|
  = f.error_notification
  / = display_base_errors @city

  .form-inputs
    = f.input :name
    = f.association :region, :selected => params[:region]

  .form-actions
    = f.button :submit, :class => 'btn-u btn-u-blue'
