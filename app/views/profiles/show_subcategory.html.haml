%h4 Список организаций

/ = search_form_for @q, :url => profiles_subcategory_path(params[:id].to_i), :class => "well well-small searchform" do |f|
/ 	.searchblock
/ 		= f.label :name_cont, 'Поиск по наименованию'
/ 		= f.text_field :name_cont
/ 	/ = f.label :city_id_eq, "Фильтр по населенному пункту"
/ 	/ = f.collection_select :city_id_eq, @cities, :id, :name, {:include_blank => "Все"}
/ 	.searchblock
/ 		= f.label :region_id_eq, "Фильтр по району"
/ 		= f.collection_select :region_id_eq, @regions, :id, :name, {:include_blank => "Все"}
/ 	.clearfix
/ 	= f.submit "Применить", class: "btn btn-small"

= render 'profiles'