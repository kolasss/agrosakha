= simple_form_for(@subcategory) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.association :category, :selected => params[:category] ? params[:category] : @subcategory.category_id
    = f.input :default_image

  .form-actions
    = f.button :submit, :class => 'btn-u btn-u-blue'
