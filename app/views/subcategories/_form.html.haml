= simple_form_for(@subcategory) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.association :category, :selected => params[:category]

  .form-actions
    = f.button :submit
