%h1 Новости

%table.table
  %tr
    %th Заголовок
    %th
    %th
    %th

  - @posts.each do |post|
    %tr
      %td= post.title
      %td= link_to 'Show', post
      %td
        - if can? :update, post
          = link_to 'Edit', edit_post_path(post)
      %td
        - if can? :destroy, post
          = link_to 'Destroy', post, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br
- if can? :create, Post
  = link_to 'Добавить новость', new_post_path, class: 'btn'