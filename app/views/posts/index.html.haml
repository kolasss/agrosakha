%h3 Статьи

- @posts.each do |post|
  .post  
    %h4
      = link_to post.title, post
      - if can? :update, post
        = link_to 'Изменить', edit_post_path(post), class: "btn btn-mini headbtn"
      - if can? :destroy, post
        = link_to 'Удалить', post, :method => :delete, :data => { :confirm => 'Вы уверены?' }, class: "btn btn-mini btn-danger headbtn"
    %p
      = raw post.shorten
    .clearfix
      = link_to 'Подробнее', post, class: "btn pull-right"

%br
- if can? :create, Post
  = link_to 'Добавить статью', new_post_path, class: 'btn'